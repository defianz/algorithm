package Programmers.Level3.외벽점검;

class Solution_Solving {
    public int solution(int n, int[] weak, int[] dist) {
        int answer = 0;
        double weakDist = (double) n / (double) weak.length;
        for (int i = 0; i < weak.length; i++) {

        }
        return answer;
    }
}